# git-create-branch

新しいブランチを作成する

**参照:** `.claude/skills/git-conventions.md`のブランチ命名規則に従う

## 実行手順

1. **状態確認**
   ```bash
   git status
   git branch -a
   ```

2. **ブランチ作成**
   ```bash
   git checkout -b <branch-name>
   git branch --show-current
   ```
